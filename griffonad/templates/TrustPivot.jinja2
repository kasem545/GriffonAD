# Pivot from {{parent.obj.name}} into trusted domain {{target.name}}
# Example operators:
# - use foreign group membership / SIDHistory in the trusted domain
# - request inter-realm tickets and continue with domain-specific ACL abuse
